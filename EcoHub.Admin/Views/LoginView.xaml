<UserControl x:Class="EcoHub.Admin.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource EhLoginBackground}">
        <!-- Decorative radial highlights -->
        <Ellipse Width="520" Height="520" Fill="#22FFFFFF" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-140,-120,0,0" />
        <Ellipse Width="380" Height="380" Fill="#1AFFFFFF" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-80,-100" />
        <Ellipse Width="140" Height="140" Fill="#33FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,60,160,0" />

        <!-- Branding side -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="80,0,0,0" MaxWidth="440" Opacity="0">
            <StackPanel.RenderTransform>
                <TranslateTransform Y="0"/>
            </StackPanel.RenderTransform>
            <StackPanel.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource EhFadeInUp}"/>
                </EventTrigger>
            </StackPanel.Triggers>
            <TextBlock Text="🥬" FontSize="64" Foreground="White" Margin="0,0,0,10" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="0"/>
                </TextBlock.RenderTransform>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard Storyboard="{StaticResource EhFloat}"/>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
            <TextBlock Text="EcoHub Admin"
                       FontSize="42" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
            <TextBlock Text="Manage your fresh food marketplace with ease."
                       FontSize="16" Foreground="#DDFFFFFF" TextWrapping="Wrap" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Border Background="#33FFFFFF" CornerRadius="12" Padding="14,8" Margin="0,0,10,0">
                    <TextBlock Text="📊 Analytics" Foreground="White" FontWeight="SemiBold" FontSize="12.5"/>
                </Border>
                <Border Background="#33FFFFFF" CornerRadius="12" Padding="14,8" Margin="0,0,10,0">
                    <TextBlock Text="📦 Inventory" Foreground="White" FontWeight="SemiBold" FontSize="12.5"/>
                </Border>
                <Border Background="#33FFFFFF" CornerRadius="12" Padding="14,8">
                    <TextBlock Text="🧾 Orders" Foreground="White" FontWeight="SemiBold" FontSize="12.5"/>
                </Border>
            </StackPanel>
        </StackPanel>

        <!-- Login card -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,80,0"
                Width="400"
                Background="White"
                CornerRadius="22"
                Padding="36,32"
                Opacity="0"
                RenderTransformOrigin="0.5,0.5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="45" ShadowDepth="10" Opacity="0.3" Color="#FF0D3D10"/>
            </Border.Effect>
            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource EhScaleIn}"/>
                </EventTrigger>
            </Border.Triggers>
            <StackPanel>
                <TextBlock Text="Welcome back" FontSize="22" FontWeight="Bold"
                           Foreground="{StaticResource EhInk900Brush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Sign in to manage your store" FontSize="12.5"
                           Foreground="{StaticResource EhInk500Brush}" HorizontalAlignment="Center" Margin="0,4,0,24"/>

                <TextBlock Text="Email" Style="{StaticResource EhLabel}"/>
                <TextBox x:Name="EmailTextBox" Style="{StaticResource EhTextBox}" Margin="0,0,0,14"/>

                <TextBlock Text="Password" Style="{StaticResource EhLabel}"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource EhPasswordBox}" Margin="0,0,0,20"/>

                <Button Content="Sign In" Click="LoginButton_Click" Style="{StaticResource EhPrimaryButton}"/>

                <TextBlock x:Name="ErrorTextBlock" Foreground="{StaticResource EhCoralBrush}"
                           Margin="0,14,0,0" TextWrapping="Wrap" TextAlignment="Center" FontSize="12"/>

                <TextBlock Margin="0,16,0,0" HorizontalAlignment="Center"
                           Foreground="{StaticResource EhInk500Brush}" FontSize="11.5">
                    <Run Text="EcoHub © 2025"/>
                    <Run Text="  •  "/>
                    <Run Text="Fresh Food Marketplace"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
