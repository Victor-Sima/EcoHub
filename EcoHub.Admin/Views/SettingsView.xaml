<UserControl x:Class="EcoHub.Admin.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource EhBgBrush}"
             d:DesignHeight="600" d:DesignWidth="940">
    <Grid Margin="28,26,28,28" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform Y="0"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource EhFadeInUp}"/>
            </EventTrigger>
        </Grid.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="System Settings" FontSize="28" FontWeight="Bold" Foreground="{StaticResource EhInk900Brush}"/>
            <TextBlock Text="Global configuration values" Foreground="{StaticResource EhInk500Brush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource EhCard}" Padding="0">
            <DataGrid x:Name="SettingsGrid" Style="{StaticResource EhDataGrid}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="240" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="200"/>
                    <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt}" Width="180" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Button Grid.Row="2" Content="💾 Save Changes" Click="Save_Click"
                Style="{StaticResource EhPrimaryButton}"
                HorizontalAlignment="Right" Margin="0,16,0,0"/>
    </Grid>
</UserControl>
