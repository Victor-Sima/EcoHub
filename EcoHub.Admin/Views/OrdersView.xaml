<UserControl x:Class="EcoHub.Admin.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource EhBgBrush}"
             d:DesignHeight="600" d:DesignWidth="940">
    <Grid Margin="28,26,28,28" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform Y="0"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource EhFadeInUp}"/>
            </EventTrigger>
        </Grid.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="Orders" FontSize="28" FontWeight="Bold" Foreground="{StaticResource EhInk900Brush}"/>
            <TextBlock Text="Customer orders and fulfilment status" Foreground="{StaticResource EhInk500Brush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource EhCard}" Padding="0">
            <DataGrid x:Name="OrdersGrid" Style="{StaticResource EhDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding UserEmail}" Width="180"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat={}{0:N2} MDL}" Width="130"/>
                    <DataGridTemplateColumn Header="Actions" Width="240">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <ComboBox x:Name="StatusCombo" Width="120" Margin="0,0,6,0" SelectedItem="{Binding Status}">
                                        <ComboBoxItem Content="New"/>
                                        <ComboBoxItem Content="Processing"/>
                                        <ComboBoxItem Content="Shipping"/>
                                        <ComboBoxItem Content="Completed"/>
                                        <ComboBoxItem Content="Cancelled"/>
                                    </ComboBox>
                                    <Button Content="Update" Click="UpdateStatus_Click" Tag="{Binding}"
                                            Background="{StaticResource EhGreen500Brush}" Foreground="White"
                                            BorderThickness="0" FontWeight="SemiBold" Padding="10,4" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
