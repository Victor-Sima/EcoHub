<UserControl x:Class="EcoHub.Admin.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource EhBgBrush}"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="28,26,28,28" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform Y="0"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource EhFadeInUp}"/>
            </EventTrigger>
        </Grid.Triggers>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="Users" FontSize="28" FontWeight="Bold" Foreground="{StaticResource EhInk900Brush}"/>
            <TextBlock Text="All registered customer accounts" Foreground="{StaticResource EhInk500Brush}" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource EhCard}" Padding="0">
            <DataGrid x:Name="UsersGrid" Style="{StaticResource EhDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="130"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="130"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="90"/>
                    <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="150"/>
                    <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginAt}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
